# 🤖 Guía de Herramientas IA para Desarrollo

## 🎯 Objetivo

Esta guía te enseña a usar herramientas de IA para acelerar el desarrollo del proyecto. Con el setup correcto, puedes reducir el tiempo de desarrollo en 30-40%.

---

## 📊 Stack de IA

### Herramientas Usadas en el Proyecto

> **Nota:** El análisis inicial evaluó Cursor IDE, GitHub Copilot, v0.dev y Codeium como stack recomendado. Durante el desarrollo del proyecto se optó por **Claude** y **Claude Code** como herramientas principales, aprovechando el acceso directo al codebase y la capacidad de actuar en múltiples archivos de forma autónoma. Esto **redujo el coste a una única suscripción mensual**, ya que Claude Code está incluido en el plan Pro de Claude.

```
CLI en terminal:      Claude Code (incluido en Claude Pro)
Chat / Arquitectura:  Claude Web (claude.ai)

TOTAL: $20/mes (Claude Pro) — ambas herramientas incluidas
```

### Setup Evaluado Inicialmente (referencia)

```
Editor Principal:     Cursor IDE ($20/mes)
Autocompletado:       GitHub Copilot ($10/mes) o Codeium (gratis)
UI/Componentes:       v0.dev (gratis tier)
Consultas:            Claude (claude.ai)
Código Boilerplate:   Copilot Chat / Cursor Composer

TOTAL: $30/mes (o $20 solo Cursor)
ROI: 30-40% más rápido = 40-50h ahorradas/mes
```

---

## 1️⃣ CURSOR IDE - Editor con IA Integrada

### ¿Qué es Cursor?

Editor fork de VSCode con IA nativa. Mejor que VSCode + Copilot juntos.

**Precio:** $20/mes (Pro)  
**Web:** [cursor.sh](https://cursor.sh)

### Setup Inicial

```bash
# 1. Descargar e instalar
# https://cursor.sh/

# 2. Migrar settings desde VSCode
# Al abrir primera vez, Cursor pregunta si importar

# 3. Login con GitHub

# 4. Configurar API key (Settings → AI)
# Usar Claude API o dejar default
```

### Características Principales

#### A) Cmd+K - Inline Editing

Edita código con lenguaje natural:

```typescript
// Selecciona este código y presiona Cmd+K (Mac) o Ctrl+K (Win)
// Prompt: "add error handling and logging"

function createOrder(data: OrderData) {
  const order = await orderRepository.create(data);
  return order;
}

// Cursor genera:
async function createOrder(data: OrderData) {
  try {
    logger.info('Creating order', { data });
    const order = await orderRepository.create(data);
    logger.info('Order created successfully', { orderId: order.id });
    return order;
  } catch (error) {
    logger.error('Failed to create order', { error, data });
    throw new OrderCreationError('Failed to create order', { cause: error });
  }
}
```

**Casos de Uso:**
- "add typescript types"
- "convert to async/await"
- "add input validation"
- "optimize this query"
- "add error handling"

---

#### B) Cmd+L - Chat con Codebase

Chat que entiende TODO tu código:

```
You: "How is authentication implemented?"
Cursor: [Analiza tu código]
"Authentication uses JWT tokens with ASP.NET Identity..."

You: "Show me where users are created"
Cursor: [Te lleva al código]
"Users are created in UserService.cs, line 42..."

You: "Add 2FA to the login flow"
Cursor: [Genera código específico para tu proyecto]
```

**Ventajas:**
- Entiende contexto completo
- Sugiere código que funciona con tu arquitectura
- Responde basándose en tu código real

---

#### C) Composer - Ediciones Multi-Archivo

Editar múltiples archivos a la vez:

```
Prompt en Composer:
"Create a new Product API endpoint with:
- Controller in API/Controllers
- Service in Core/Services  
- Tests in Tests/Integration
- Update Swagger docs"

Cursor genera y modifica:
✓ ProductController.cs (nuevo)
✓ ProductService.cs (nuevo)
✓ IProductService.cs (nuevo)
✓ ProductControllerTests.cs (nuevo)
✓ Program.cs (actualizado, registra servicio)
✓ Startup.cs (actualizado, añade route)
```

**Casos de Uso:**
- Crear features completas
- Refactorizar múltiples archivos
- Añadir tests a varios archivos
- Actualizar imports en todo el proyecto

---

### Prompts Efectivos en Cursor

#### Para Componentes React:

```
"Create a ProductCard component with:
- Image with lazy loading
- Title, price, and description
- Add to cart button
- TypeScript props
- Tailwind styling
- Responsive design"
```

#### Para APIs .NET:

```
"Create CRUD endpoints for Orders:
- GET /api/orders (with pagination)
- GET /api/orders/{id}
- POST /api/orders
- PUT /api/orders/{id}
- DELETE /api/orders/{id}
- Include FluentValidation
- Add XML comments for Swagger"
```

#### Para Tests:

```
"Generate unit tests for OrderService:
- Test CreateOrder with valid data
- Test CreateOrder with invalid data
- Test GetOrder by id (found and not found)
- Mock repository
- Use xUnit and FluentAssertions"
```

---

## 2️⃣ GITHUB COPILOT - Autocompletado Inteligente

### Setup

```bash
# 1. Instalar extensión en Cursor/VSCode
# Extensions → "GitHub Copilot"

# 2. Login con cuenta GitHub

# 3. Activar ($10/mes si no tienes Copilot gratis)
```

### Cómo Usar

#### Autocompletado en Línea

Simplemente empieza a escribir y Copilot sugiere:

```typescript
// Escribes:
function calculateShipping

// Copilot sugiere (presiona Tab para aceptar):
function calculateShippingCost(zone: ShippingZone, weight: number): number {
  const baseRate = zone.baseRate;
  const perKgRate = zone.perKgRate;
  return baseRate + (weight * perKgRate);
}
```

#### Generar desde Comentarios

```typescript
// Write a function that validates email format
// Copilot genera:
function validateEmail(email: string): boolean {
  const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  return emailRegex.test(email);
}

// Create a hook to manage shopping cart state
// Copilot genera:
function useShoppingCart() {
  const [items, setItems] = useState<CartItem[]>([]);
  
  const addItem = (item: CartItem) => {
    setItems([...items, item]);
  };
  
  const removeItem = (id: string) => {
    setItems(items.filter(item => item.id !== id));
  };
  
  return { items, addItem, removeItem };
}
```

#### Copilot Chat

```
En panel lateral:
"Explain this code"
"Find bugs"
"Add error handling"
"Generate tests"
"Optimize performance"
```

---

### Tips para Mejores Sugerencias

1. **Nombres descriptivos:**
   ```typescript
   // ❌ Malo
   function calc(a, b)
   
   // ✅ Bueno
   function calculateTotalWithTax(subtotal: number, taxRate: number)
   ```

2. **Comentarios claros:**
   ```typescript
   // ❌ Malo
   // do stuff
   
   // ✅ Bueno
   // Calculate shipping cost based on weight and destination zone
   // Returns cost in euros
   ```

3. **Contexto en el archivo:**
   - Copilot lee el archivo actual
   - Define types/interfaces arriba
   - Importa dependencias antes

---

## 3️⃣ V0.DEV - Generador de Componentes UI

### ¿Qué es v0?

Herramienta de Vercel que genera componentes React desde descripciones.

**Precio:** Gratis (con límites) / $20/mes  
**Web:** [v0.dev](https://v0.dev)

### Casos de Uso

#### Generar Landing Pages

```
Prompt:
"Modern landing page for a sim racing shop with:
- Hero section with CTA
- Product grid (3 columns)
- Features section with icons
- Testimonials
- Footer with links
- Dark mode
- Tailwind CSS"

→ v0 genera código listo para copiar
```

#### Generar Componentes

```
Prompt:
"Product card component with:
- Image with hover zoom
- Product name and price
- Add to cart button
- Rating stars
- Badge for 'New' or 'Sale'
- Responsive
- shadcn/ui components"

→ Genera componente completo
```

#### Generar Formularios

```
Prompt:
"Checkout form with:
- Shipping address fields
- Payment method selection
- Order summary sidebar
- Validation with react-hook-form
- Zod schema
- Error messages
- Loading states"

→ Genera formulario completo validado
```

### Workflow con v0

```
1. Describir componente en v0.dev
2. Copiar código generado
3. Pegar en tu proyecto
4. Ajustar según necesidad
5. Integrar con tu lógica
```

**Ahorro:** 1-2 horas por componente complejo

---

## 4️⃣ CLAUDE & CLAUDE CODE - Herramientas Usadas en el Proyecto

> Herramientas principales usadas durante el desarrollo. Claude Code es el CLI oficial de Anthropic que ejecuta Claude directamente en el terminal con acceso real al sistema de archivos del proyecto.

### ¿Qué es Claude Code?

CLI que corre en tu terminal y puede leer, crear y editar archivos, ejecutar comandos bash y encadenar múltiples acciones de forma autónoma. A diferencia de extensiones de editor, tiene acceso al codebase completo y actúa sobre él directamente.

**Precio:** Incluido en Claude Pro ($20/mes) / uso por API
**Docs:** [docs.anthropic.com/claude-code](https://docs.anthropic.com/claude-code)

### Setup de Claude Code

#### 1. Instalación

```bash
# Instalar globalmente
npm install -g @anthropic-ai/claude-code

# Verificar
claude --version

# Login con cuenta Anthropic
claude login
```

#### 2. Iniciar en el proyecto

```bash
# Desde la raíz del repositorio
cd sim-racing-shop
claude
```

Claude Code lee automáticamente `CLAUDE.md` al iniciarse, cargando el contexto completo del proyecto (comandos, arquitectura, URLs, workflow de Git) sin necesidad de repetirlo en cada sesión.

#### 3. CLAUDE.md — Contexto del Proyecto

El fichero `CLAUDE.md` en la raíz del repositorio es la fuente de contexto persistente de Claude Code. Contiene:

- Comandos de desarrollo (frontend, backend, Docker, EF Core)
- Arquitectura (Clean Architecture, App Router, capas)
- URLs de entornos locales
- Variables de entorno requeridas
- Convenciones de Git y commits

Cuando se añadan nuevas convenciones o cambios arquitectónicos, actualizar `CLAUDE.md` para que Claude Code los conozca automáticamente.

---

### Características Principales

#### A) Lectura y Escritura de Archivos

Claude Code lee el código real del proyecto y aplica ediciones directamente:

```
Prompt: "Lee OrderService.cs y añade manejo de errores
al método CreateOrder siguiendo el patrón de los otros servicios"

→ Claude Code:
  1. Lee OrderService.cs
  2. Lee otros servicios como referencia
  3. Propone los cambios con diff
  4. Aplica la edición directamente
```

#### B) Ejecución de Comandos

```
Prompt: "Ejecuta los tests del frontend y dime qué está fallando"

→ Claude Code ejecuta: npm run test
→ Analiza el output de Vitest
→ Identifica los tests fallidos
→ Lee los archivos implicados
→ Propone y aplica correcciones
```

#### C) Contexto de Codebase Completo

Claude Code tiene acceso real a todos los archivos, no solo al fragmento que le pegues:

```
Prompt: "¿Cómo está implementado el carrito para usuarios anónimos?"

→ Claude Code busca en el código fuente
→ Lee CartController.cs, los servicios de Redis,
   la lógica de X-Cart-Session header
→ Responde basándose en la implementación real
```

#### D) Modo Agente — Acciones Multi-Paso

Puede encadenar múltiples acciones de forma autónoma:

```
Prompt: "Crea un endpoint CRUD para Wishlist siguiendo
el patrón del CartController:
- Controlador en API/Controllers
- Repositorio en Infrastructure/Repositories
- Tests unitarios en SimRacingShop.Tests"

→ Claude Code:
  1. Explora la estructura existente
  2. Lee CartController.cs como referencia
  3. Crea WishlistController.cs
  4. Crea IWishlistRepository.cs y WishlistRepository.cs
  5. Crea WishlistControllerTests.cs
  6. Actualiza Program.cs con el registro del servicio
```

---

### Casos de Uso en Este Proyecto

#### Documentación Técnica

```
"Actualiza docs/architecture/02. database-design.md
basándote en las entidades reales del backend.
Usa diagramas Mermaid, no SQL."

→ Claude Code lee todas las entidades C# y el AppDbContext
   y reescribe la documentación con precisión
```

#### Generación de Tests

```
"Genera tests Vitest para el componente ProductCard
siguiendo el patrón de tests/components/ui/.
Usa el helper render.tsx de tests/helpers/"

→ Claude Code lee los tests existentes como referencia
   y genera tests consistentes con el estilo del proyecto
```

#### Code Review con Contexto Real

```
"Revisa PaymentController.cs y comprueba que sigue
los mismos patrones que el resto de controllers:
validación con FluentValidation, ActionResult, [Authorize]"

→ Claude Code compara con otros controllers
   e indica discrepancias
```

#### Debugging

```
"El test LoginPage.test.tsx falla con este error:
[pegar error]
Busca la causa y corrígela"

→ Claude Code lee el test, el componente, el setup.ts
   y aplica la corrección
```

#### Migraciones EF Core

```
"Añade el campo 'trackingNumber' (string, nullable) a la entidad Order.
Actualiza el DTO de respuesta y la configuración de EF Core"

→ Claude Code modifica Order.cs, OrderDto.cs, OrderConfiguration.cs
   y genera el comando de migración para ejecutar
```

---

### Prompts Efectivos con Claude Code

#### Explorar el Proyecto

```
"¿Qué endpoints expone el backend?
Lista todos los controllers y sus rutas HTTP"
```

```
"¿Cómo está organizada la autenticación en el frontend?
Explica el flujo desde el login hasta la sesión persistida"
```

#### Añadir Features

```
"Siguiendo el patrón del CartController, crea un WishlistController con:
- GET /api/wishlist
- POST /api/wishlist/{productId}
- DELETE /api/wishlist/{productId}"
```

```
"Añade soporte de paginación al endpoint GET /api/products.
Usa el mismo patrón de PagedResult que exista en el proyecto"
```

#### Tests

```
"Genera tests xUnit para OrderService con:
- Moq para el repositorio
- FluentAssertions para las aserciones
- Cubre happy path y casos de error"
```

```
"Genera tests Vitest para el hook useCart
siguiendo el patrón de tests/stores/
Usa vi.mock para las llamadas a la API"
```

#### Documentación

```
"Actualiza docs/development/04. testing-plan.md
basándote en los tests que existen actualmente en
frontend/tests/ y backend/SimRacingShop.Tests/"
```

---

### Claude Web (claude.ai) — Chat

Para tareas que no requieren acceso al código local:

#### Decisiones de Arquitectura

```
"Estoy construyendo un carrito con Redis.
Para usuarios anónimos uso session IDs, para autenticados
uso el userId. ¿Cuál es la mejor estrategia para mergear
el carrito anónimo al autenticarse?"

→ Claude razona sobre trade-offs y propone la estrategia
```

#### Dudas Conceptuales

```
"¿Cuál es la diferencia entre Squash Merge y Merge Commit?"
"¿Cómo funciona el middleware de autorización en ASP.NET?"
"¿Qué ventajas tiene Testcontainers sobre mocks en tests de BD?"
```

#### Revisión de Snippets

```
"Revisa este código C#:
[pegar código]
Busca problemas de:
- Seguridad
- async/await mal usado
- Patrones incorrectos"
```

---

### Cuándo Usar Cada Herramienta

| Tarea | Claude Code | Claude Web |
|-------|:-----------:|:----------:|
| Editar archivos del proyecto | ✅ | ❌ |
| Ejecutar comandos y ver output | ✅ | ❌ |
| Contexto del codebase real | ✅ | ❌ |
| Decisiones de arquitectura | ⚠️ | ✅ |
| Preguntas conceptuales | ⚠️ | ✅ |
| Revisión de snippets aislados | ✅ | ✅ |
| Generar documentación | ✅ | ⚠️ |

> ⚠️ Posible pero no óptimo

---

## 5️⃣ CODEIUM - Alternativa Gratuita a Copilot

### ¿Por qué Codeium?

Si no quieres pagar Copilot, Codeium es excelente y **100% gratis**.

**Precio:** $0  
**Web:** [codeium.com](https://codeium.com)

### Setup

```bash
# 1. Instalar extensión en Cursor/VSCode
# Extensions → "Codeium"

# 2. Crear cuenta gratis

# 3. Usar igual que Copilot
```

### Características

- Autocompletado inline (como Copilot)
- Chat con código
- Explicaciones
- Generación de tests
- Múltiples lenguajes

**Diferencia vs Copilot:**
- Ligeramente menos preciso
- Pero gratis e ilimitado
- Excelente para empezar

---

## 6️⃣ WORKFLOWS CON IA - Ejemplos Prácticos

### Workflow: Crear Feature Completo

```
1. Arquitectura (Claude Web):
   "Diseña la estructura para una wishlist:
   entidades, endpoints y lógica de negocio necesaria"
   → Obtener la estrategia y estructura

2. Backend (Claude Code):
   "Crea el WishlistController y repositorio
   siguiendo el patrón del CartController"
   → Genera controllers, repositorio, registro en DI

3. Frontend (Claude Code):
   "Crea el hook useWishlist y la página de wishlist
   siguiendo el estilo de useCart y CartPage"
   → Genera hooks, componentes y llamadas a la API

4. Tests (Claude Code):
   "Genera tests xUnit para WishlistController
   y tests Vitest para useWishlist"
   → Tests coherentes con los patrones del proyecto

5. Review (Claude Code):
   "Revisa toda la implementación de wishlist y
   comprueba que sigue los patrones del resto del proyecto"
   → Code review final con contexto real
```

**Tiempo sin IA:** 8-10 horas
**Tiempo con IA:** 3-4 horas
**Ahorro:** 50-60%

---

### Workflow: Debugging de Producción

```
1. Recopilar info (Manual):
   - Error logs de Seq (localhost:5341)
   - Stack trace
   - Request details
   - Environment

2. Analizar (Claude Code):
   "Tengo este error en producción:
   [logs + stack trace]
   Busca en el código la causa raíz"
   → Claude Code busca en controllers, servicios y middleware

3. Reproducir localmente (Claude Code):
   "Añade un test que reproduzca este error
   en el fichero [TestFile].cs"
   → Genera test de reproducción

4. Fix (Claude Code):
   "Corrige el bug en [Archivo].cs
   teniendo en cuenta que..."
   → Aplica el fix directamente

5. Test de regresión (Claude Code):
   "Añade un test de regresión que verifique
   que este bug no vuelve a ocurrir"
   → Genera test de regresión

6. Verificar (Manual):
   Ejecutar tests localmente, deploy a staging
```

---

### Workflow: Refactoring Grande

```
1. Plan (Claude Web):
   "Necesito refactorizar OrderService para extraer
   la lógica de pago. Objetivos:
   - Separar PaymentService
   - Mejorar testabilidad
   - Mejor manejo de errores
   Propón un plan paso a paso"
   → Plan de refactoring detallado

2. Ejecutar (Claude Code):
   "Implementa este plan de refactoring en el proyecto:
   [pegar plan]
   Empieza por OrderService.cs"
   → Claude Code modifica múltiples archivos

3. Tests (Claude Code):
   "Genera tests unitarios para el código refactorizado.
   Revisa el test anterior en OrderServiceTests.cs
   y actualiza para la nueva estructura"
   → Tests adaptados a la nueva arquitectura

4. Review (Claude Code):
   "Revisa el refactoring completo y comprueba
   que no haya regresiones respecto al comportamiento anterior"
   → Final check con contexto real del codebase
```

---

## 🎯 Prompts Efectivos - Cheat Sheet

### Para Componentes React

```
"Create a [ComponentName] component with:
- Props: [list props and types]
- Features: [list features]
- Styling: Tailwind CSS, [specific styles]
- State management: [if needed]
- Accessibility: [if important]
- TypeScript with full type safety"
```

### Para APIs .NET

```
"Create [endpoint description]:
- HTTP method: [GET/POST/etc]
- Route: /api/[route]
- Input: [DTO structure]
- Output: [response structure]
- Validation: FluentValidation
- Error handling: try-catch with specific exceptions
- Authorization: [if needed]
- XML comments for Swagger"
```

### Para Tests

```
"Generate [type] tests for [class name]:
- Framework: [xUnit/NUnit]
- Assertions: [FluentAssertions]
- Mocking: [Moq/NSubstitute]
- Coverage:
  * Happy path
  * Edge cases: [list specific cases]
  * Error scenarios: [list]
- Use AAA pattern (Arrange, Act, Assert)"
```

### Para Base de Datos

```
"Design database schema for [feature]:
- Tables needed: [list]
- Relationships: [describe]
- Constraints: [list]
- Indexes: [what needs indexing]
- Consider: [specific requirements]
- Generate: PostgreSQL SQL script with:
  * CREATE TABLE statements
  * Foreign keys
  * Indexes
  * Sample data INSERT"
```

### Para Debugging

```
"Debug this issue:

Error: [paste error message]

Code: [paste relevant code]

Context: [what you were trying to do]

Already tried: [list what you tried]

Please:
1. Identify root cause
2. Explain why it's happening
3. Provide step-by-step fix
4. Suggest how to prevent similar issues"
```

---

## 💡 Tips Avanzados

### 1. Contexto es Clave

**❌ Malo:**
```
"create login function"
```

**✅ Bueno:**
```
"Create login function for our Next.js app:
- Uses JWT authentication
- Calls /api/auth/login endpoint
- Stores token in httpOnly cookie
- Redirects to dashboard on success
- Shows error toast on failure
- TypeScript with Zod validation
- Use our existing axios instance from @/lib/api"
```

### 2. Iteración Incremental

```
1. "Create basic ProductCard component"
2. "Add hover effects and animations"
3. "Add lazy loading for images"
4. "Add add-to-cart functionality"
5. "Add loading and error states"
```

Mejor que todo de una vez.

### 3. Aprovechar el Código Existente

```
"Refactor this to use our existing useCart hook:
[pegar código]

Our useCart hook:
[pegar hook]"
```

Cursor/Claude entienden mejor con ejemplos.

### 4. Especificar Estilo

```
"Use our coding style:
- Functional components (no class)
- TypeScript strict mode
- Tailwind for styling (no CSS modules)
- Named exports
- Arrow functions
- Descriptive variable names"
```

### 5. Pedir Explicaciones

```
"Explain this code step by step:
[código complejo]

Then suggest improvements."
```

---

## 📊 Métricas y ROI

### Tiempo Ahorrado por Tarea

| Tarea | Sin IA | Con IA | Ahorro |
|-------|--------|--------|--------|
| CRUD API completo | 4h | 1h | 75% |
| Componente UI complejo | 2h | 30min | 75% |
| Tests unitarios | 2h | 45min | 63% |
| Debugging bug complejo | 3h | 1h | 67% |
| Refactoring grande | 6h | 2h | 67% |
| Documentación | 2h | 30min | 75% |

### ROI Mensual

```
Inversión IA: $20/mes (Claude Pro — CLI + chat incluidos)

Ahorro tiempo: 40h/mes (conservador)
Valor tiempo: $60/h
Ahorro dinero: $2,400/mes

ROI: 120x (12000%)
```

**Incluso con solo 10h ahorradas/mes:**
```
10h × $60/h = $600/mes ahorro
Inversión: $20/mes (una sola suscripción)
ROI: 30x (3000%)
```

---

## ⚠️ Limitaciones y Cuidados

### Lo que IA NO Hace Bien

1. **Arquitectura de Alto Nivel**
   - IA ayuda, pero decisiones importantes son tuyas
   - Revisar propuestas críticamente

2. **Seguridad Crítica**
   - Siempre revisar código relacionado con auth
   - Validar inputs manualmente
   - No confiar ciegamente en validaciones generadas

3. **Lógica de Negocio Compleja**
   - IA puede no entender contexto completo
   - Revisar cálculos, reglas de negocio

4. **Performance Optimization**
   - IA genera código funcional, no siempre óptimo
   - Hacer profiling y optimizar manualmente

### Best Practices

✅ **DO:**
- Revisar TODO el código generado
- Testear exhaustivamente
- Usar IA como asistente, no como sustituto
- Aprender del código generado
- Iterar y refinar prompts

❌ **DON'T:**
- Copiar/pegar sin entender
- Confiar en código crítico sin revisar
- Usar en producción sin testear
- Depender 100% de IA
- Ignorar warnings/errores

---

## 🎓 Recursos para Aprender Más

### Prompting

- [Anthropic Prompt Engineering](https://docs.anthropic.com/claude/docs/prompt-engineering)
- [OpenAI Best Practices](https://platform.openai.com/docs/guides/prompt-engineering)
- [Cursor Documentation](https://cursor.sh/docs)

### Ejemplos

- [Cursor Directory](https://cursor.directory/) - Prompts comunitarios
- [Awesome ChatGPT Prompts](https://github.com/f/awesome-chatgpt-prompts)

### Comunidades

- [Cursor Discord](https://discord.gg/cursor)
- [r/cursor](https://reddit.com/r/cursor)
- [r/ChatGPTCoding](https://reddit.com/r/ChatGPTCoding)

---

## 📋 Checklist Setup IA

Antes de empezar desarrollo con IA:

- [ ] Node.js instalado (requerido para Claude Code)
- [ ] Claude Code instalado: `npm install -g @anthropic-ai/claude-code`
- [ ] Login realizado: `claude login`
- [ ] Cuenta Claude Pro activa (o créditos de API)
- [ ] [claude.ai](https://claude.ai) bookmarked para consultas web
- [ ] Verificar que `CLAUDE.md` está actualizado en la raíz del repo
- [ ] Primera sesión de prueba: `cd sim-racing-shop && claude`
- [ ] Familiarizado con flujo: prompt → revisión del diff → aprobación

---

## 🚀 Primeros Pasos

### Día 1: Setup y Familiarización

1. Instalar Claude Code: `npm install -g @anthropic-ai/claude-code`
2. Hacer login: `claude login`
3. Iniciar en el proyecto: `cd sim-racing-shop && claude`
4. Probar exploración: "¿Qué endpoints tiene el backend?"
5. Probar edición: "Añade un comentario XML al método más simple que encuentres"

### Día 2: Primera Feature con IA

1. Elegir una tarea pequeña (ej: añadir un campo a un DTO)
2. Planear con Claude Web si hay dudas de enfoque
3. Implementar con Claude Code: "Añade el campo X a Y siguiendo el patrón de Z"
4. Revisar los diffs propuestos antes de aprobar
5. Ejecutar tests para verificar: `npm run test` / `dotnet test`

### Semana 1: Workflow Establecido

1. Familiarizarse con el ciclo: prompt → diff → aprobación → test
2. Construir un banco de prompts frecuentes para este proyecto
3. Actualizar `CLAUDE.md` con cualquier convención nueva descubierta
4. Identificar qué tareas se benefician más de Claude Code vs Claude Web

---

**Última actualización:** Marzo 2026
**Mantener actualizado con nuevas herramientas y convenciones del proyecto**
